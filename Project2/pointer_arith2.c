/*#include <stdio.h>

int main(void)
{
	int i = 10;
	int* pi = &i;

	printf("i = %d, pi = %p\n", i, pi);
	(*pi)++; //p가 가리키는 값을 1증가;
	printf("i = %d, pi = %p\n", i, pi);

	printf("i = %d, pi = %p\n", i, pi);
	*pi++; //p(주소)를 1증가;
	printf("i = %d, pi = %p\n", i, pi);

	return 0;
}*/